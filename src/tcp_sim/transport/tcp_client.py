"""TCP client transport placeholder for Phase 2."""

import asyncio


class TcpClient:
    """Placeholder for asyncio TCP client with reconnect support."""

    async def start(self) -> None:
        await asyncio.sleep(0)

    async def stop(self) -> None:
        await asyncio.sleep(0)
