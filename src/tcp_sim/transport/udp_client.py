"""UDP client transport placeholder for Phase 3."""

import asyncio


class UdpClient:
    """Placeholder for UDP client sender behavior."""

    async def start(self) -> None:
        await asyncio.sleep(0)

    async def stop(self) -> None:
        await asyncio.sleep(0)
