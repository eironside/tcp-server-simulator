"""UDP server transport placeholder for Phase 3."""

import asyncio


class UdpServer:
    """Placeholder for UDP multicast and reply-to-senders behavior."""

    async def start(self) -> None:
        await asyncio.sleep(0)

    async def stop(self) -> None:
        await asyncio.sleep(0)
