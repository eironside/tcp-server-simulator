"""TCP server transport placeholder for Phase 2."""

import asyncio


class TcpServer:
    """Placeholder for asyncio TCP server broadcast implementation."""

    async def start(self) -> None:
        await asyncio.sleep(0)

    async def stop(self) -> None:
        await asyncio.sleep(0)
